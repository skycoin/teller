package rpc

import (
	"testing"
)

func TestSend(t *testing.T) {
	// cur, err := user.Current()
	// assert.Nil(t, err)
	// f := filepath.Join(cur.HomeDir, ".skycoin/wallets/skycoin.wlt")
	// cli := New(f, "127.0.0.1:7430")
	// txid, err := cli.Send("cBB6zToAjhMkvRrWaz8Tkv6QZxjKCfJWBx", 1)
	// assert.Nil(t, err)
	// fmt.Println(txid)
}

func TestGetTransaction(t *testing.T) {
	// cur, err := user.Current()
	// assert.Nil(t, err)
	// f := filepath.Join(cur.HomeDir, ".skycoin/wallets/skycoin.wlt")
	// cli := New(f, "127.0.0.1:6430")

	// tx, err := cli.GetTransaction("08c863809e56c0336d4a9df4c518007740534a5796e7b530daf6b7c9e48afde8")
	// assert.Nil(t, err)

	// tx, err = cli.GetTransaction("08c863809e56c0336d4a9df4c518007740534a5796e7b530daf6b7c9e48afde9")
	// assert.Nil(t, err)
	// assert.Nil(t, tx.Transaction.Txn)
}
